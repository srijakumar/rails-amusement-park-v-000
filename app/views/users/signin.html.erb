<%= form_for(:user, url: {controller: :sessions, action: :create}) do |f| %>

  <%=f.collection_select :name, User.all, :name, "name" %>
  <%=f.text_field :name %>

  <%=f.text_field :password, type: :password, id: "password" %>

  <%=f.submit "Sign In" %>

<% end %>
